docker rmi $(docker images -a -q)

# ----- Prompt ----------------------------------------------------------------
# Delete all the container images stored on the Char virtual machine, in one
# command as well.
# -----------------------------------------------------------------------------

# ----- Notes -----------------------------------------------------------------
# rmi is for deleting images
# -----------------------------------------------------------------------------
