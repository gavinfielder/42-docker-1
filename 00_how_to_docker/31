docker service rm $(docker service ls -q)

# ----- Prompt ----------------------------------------------------------------
# Force quit and delete all the services on the local swarm, in one command
# -----------------------------------------------------------------------------

# ----- Notes -----------------------------------------------------------------
# The -q option of docker service ls only shows ids, which are passed to 
# docker service rm
# -----------------------------------------------------------------------------
